Problem statement
You are given two strings S and T of lengths M and N, respectively. Find the 'Edit Distance' between the strings.

Edit Distance of two strings is the minimum number of steps required to make one string equal to the other. In order to do so, you can perform the following three operations:

1. Delete a character
2. Replace a character with another one
3. Insert a character
Note :
Strings don't contain spaces in between.
Detailed explanation ( Input/output format, Notes, Images )
Constraints :
0 <= M <= 10 ^ 3
0 <= N <= 10 ^ 3

Time Limit: 1 sec
Sample Input 1 :
abc
dc
Sample Output 1 :
2
 Explanation to the Sample Input 1 :
 In 2 operations we can make string T to look like string S.
First, insert character 'a' to string T, which makes it "adc".

And secondly, replace the character 'd' of string T with 'b' from the string S. This would make string T as "abc" which is also string S. 

Hence, the minimum distance.
Sample Input 2 :
whgtdwhgtdg
aswcfg
Sample Output 2 :
9


int edith(string s1,string s2,int arr[1002][1002])
{
	if(s1.length()==0)
	{
          arr[s1.length()][s2.length()] =
              s2.length() - s1.length(); return s2.length() - s1.length();
        }
        if (s2.length() == 0) {
          arr[s1.length()][s2.length()] =  s1.length() - s2.length() ;
		  return s1.length() - s2.length();
        }
		if(s1[0]==s2[0])
		{
			if(arr[s1.length()-1][s2.length()-1]!=-1)
			{
              arr[s1.length()][s2.length()] = arr[s1.length() - 1][s2.length() - 1];         
            }
else
{
              arr[s1.length()][s2.length()]=edith(s1.substr(1),s2.substr(1),arr);
}
return arr[s1.length()][s2.length()];
                }
				else
				{
                  arr[s1.length()][s2.length()]=1+min(edith(s1.substr(1),s2,arr),min(edith(s1.substr(1),s2,arr),edith(s1.substr(1),s2.substr(1),arr)));
                  return arr[s1.length()][s2.length()];
                                }
}
int editDistance(string s1, string s2)
{
	//Write your code here
	int m=s1.length();
	int n= s2.length();
	int arr[1002][1002];
	/*for(int i=0;i<=m;i++)
	{
		for(int j=0;j<=n;j++)
		{
			arr[i][j]=-1;
		}
	}*/
	for(int i=0;i<=m;i++)
	{
		arr[i][0]=i;
	}
	for(int j=1;j<=n;j++)
	{
		arr[0][j]=j;
	}
	for(int i=1;i<=m;i++)
	{
		for(int j=1;j<=n;j++)
		{
			if(s1[m-i]==s2[n-j])
			{
				arr[i][j]=arr[i-1][j-1];
			}
			else
			{
				arr[i][j]=1+min(arr[i-1][j],min(arr[i][j-1],arr[i-1][j-1]));
			}
		}
	}
	return arr[m][n];
//	return edith(s1,s2,arr);
}
